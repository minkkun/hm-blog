/**
 * Geometry shared by the full-bleed pages that sit beside the fixed nav rail.
 * Kept in one place so the feed and the gallery cannot drift apart.
 */
export const RAIL_WIDTH = "12.5rem"

/** Clears the mobile nav strip, with room for a dropdown under the heading. */
export const RAIL_PADDING_MOBILE = "3.5rem 1.25rem 4rem"

/** Clears the fixed rail on the right and the floating wordmark above. */
export const RAIL_PADDING_DESKTOP = "9rem 1.5rem 8rem 3vw"
